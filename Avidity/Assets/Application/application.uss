#playback   { min-height: 12%; }
#content    { max-height: 86%; }
#navigation { min-height: 12%; }

#selector { min-width: 25%; background-color: rgba(0, 0, 0, 0.4); }
#window   { min-width: 50%; }
#queue    { min-width: 25%; }

.window {
    display: none;
    flex-grow: 1;
    width: 100%;
}
